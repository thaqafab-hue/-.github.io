<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>نظام العلامات - بوابة إرسال التعليمية</title>
  <style>
    body {
      font-family: "Cairo", sans-serif;
      background: linear-gradient(135deg, #2193b0, #6dd5ed);
      color: #333;
      text-align: center;
      margin: 0;
      padding: 20px;
    }
    h1 {
      color: #fff;
      font-size: 28px;
    }
    table {
      width: 95%;
      margin: 20px auto;
      border-collapse: collapse;
      background: white;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }
    th, td {
      padding: 12px;
      border-bottom: 1px solid #ccc;
    }
    th {
      background-color: #1e90ff;
      color: white;
    }
    input[type="number"] {
      width: 60px;
      padding: 4px;
      border: 1px solid #ccc;
      border-radius: 4px;
      text-align: center;
    }
    button {
      background-color: #28a745;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      margin: 10px;
    }
    button:hover {
      background-color: #218838;
    }
    .back {
      background-color: #007bff;
    }
  </style>
</head>
<body>

  <h1>📘 نظام العلامات - بوابة إرسال التعليمية</h1>

  <table id="marksTable">
    <thead>
      <tr>
        <th>ID الطالب</th>
        <th>الاسم</th>
        <th>امتحان يومي (1) /10</th>
        <th>نصف الفصل /20</th>
        <th>امتحان يومي (2) /10</th>
        <th>نهاية الفصل /40</th>
        <th>تقويم نوعي /20</th>
        <th>المجموع /100</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>1001</td>
        <td>محمد أحمد</td>
        <td><input type="number" min="0" max="10"></td>
        <td><input type="number" min="0" max="20"></td>
        <td><input type="number" min="0" max="10"></td>
        <td><input type="number" min="0" max="40"></td>
        <td><input type="number" min="0" max="20"></td>
        <td class="total">0</td>
      </tr>
      <tr>
        <td>1002</td>
        <td>ليان خالد</td>
        <td><input type="number" min="0" max="10"></td>
        <td><input type="number" min="0" max="20"></td>
        <td><input type="number" min="0" max="10"></td>
        <td><input type="number" min="0" max="40"></td>
        <td><input type="number" min="0" max="20"></td>
        <td class="total">0</td>
      </tr>
    </tbody>
  </table>

  <button onclick="calculateTotals()">احسب العلامات</button>
  <button class="back" onclick="history.back()">رجوع</button>

  <script>
    function calculateTotals() {
      const rows = document.querySelectorAll("#marksTable tbody tr");
      rows.forEach(row => {
        const inputs = row.querySelectorAll("input[type='number']");
        let total = 0;
        inputs.forEach(input => {
          total += Number(input.value) || 0;
        });
        row.querySelector(".total").textContent = total;
      });
      alert("✅ تم حساب العلامات بنجاح!");
    }
  </script>

</body>
</html>
